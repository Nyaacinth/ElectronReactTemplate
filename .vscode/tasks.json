{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Electron App",
            "type": "shell",
            "command": "bun build-app",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
